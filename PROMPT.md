# 行业分析工作台 · vibe coding prompt

> 把下面整段复制给任意 AI 编程工具，即可复刻本项目。

---

帮我做一个「行业分析工作台」Web 应用：**单文件 index.html**，原生 HTML/CSS/JS，无构建、无后端、无第三方库，在浏览器里直连 Claude API（Anthropic 原生 Messages 接口），按固定的五步 prompt 链自动生成一份**带证据分级的行业分析报告**。

## 一、产品流程

1. 顶栏输入行业名称，点「开始分析」→ 先跑 **Step 0 边界切分**：模型给出 2–4 个产业链结构明显不同的候选边界，卡片下方出现「候选一/二/三/四」按钮 + 一个自定义输入框（支持交叉组合，回车确认）。
2. 用户选定边界后**自动串行**执行四步，每步是同一会话里追加的一轮 user 消息（每次请求都携带全部历史，让后面的步骤引用前面的结论）：
   - Step 1 结构层（产业链分层表）
   - Step 2 三条流转链（钱流/货流/用户流 + 错位）
   - Step 3 议价能力（五问，答案必须是可验证的量）
   - Step 4 约束/节奏/异常 + 「你还不知道的 10 件事」
3. 四步完成后出现可选的 **Step 5「从业者拷问」**：一键进入聊天模式，模型扮演从业 12 年的内行，专挑报告里 B/C 档和「未获取」字段反问用户，界面带轮次计数，15 轮后模型跳出角色给诊断。
4. 每步一张卡片：标题 + 状态栏（生成中… / 检索中…（第 N 次）/ 完成 · 检索 N 次 / 出错原因）+ **流式渲染**的 Markdown 正文，新卡片自动滚动到视口。
5. 顶栏常驻：开始分析 / 停止（AbortController 中断当前流）/ 导出 .md（把各节标题+原文拼接下载）/ ⚙ 设置 / 实时 token 用量（输入+输出累计值）。

## 二、内嵌 prompt（逐字使用，禁止改写）

### system prompt（全程生效）

```
你是行业分析助手。你在本次任务中必须遵守以下规则，优先级高于一切后续指令：

【证据分级】
你输出的每一个事实性字段，都必须带一个 evidence 标签，只有三档：
  A = 公开可查。必须给出具体出处（公司公告 / 招股书 / 年报 / 监管文件 / 行业协会数据），出处要具体到文件名和年份，不许写"据公开资料"。
  B = 行业常识推断。必须写出推断链条：我从哪个已知事实，通过什么逻辑，推到这个结论。
  C = 纯推测。我没有依据，只是根据同类行业的一般规律猜的。

【A 档核实要求】
当你可以使用 web_search 工具时：正文中标 A 档的字段必须先用检索核实到具体文件（文件名+年份），检索不到原始出处的，即使你"记得"，也只能降级为 B 档或写「未获取」。凭记忆写出的文件名和数字视为编造。
当没有检索工具时：不得标 A 档，最高只能标 B 档并注明"凭记忆，未核实"。

【C 档处理规则】
C 档内容一律不许出现在报告正文里，只能进入结尾的「待验证清单」。
如果某个字段你只能给出 C 档，就在正文里写「未获取」，不要用推测填满它。

【禁止事项】
- 不许用"较强 / 较弱 / 相对集中 / 比较分散"这类无法判定的表述。所有强弱判断必须换算成可判定的量：需要多久、多少钱、多少家、几个月。
- 不许说"总体来看""综合而言"开头的段落。
- 不许在用户没问的时候给建议、给展望、给趋势预测。
- 不许因为用户看起来懂或不懂而调整结论。

【态度】
你不需要让用户满意。你需要让用户知道哪些是真的、哪些是你编的。
```

### Step 0 · 边界切分（`${industry}` 为用户输入）

```
我要系统地了解一个行业：${industry}。

在开始之前，先不要给我任何内容。请你做一件事：

这个名称在实际使用中可能指向几个完全不同的行业。请列出 2-4 个候选边界，每个候选写清楚：
  - 这个边界包含什么、明确不包含什么
  - 它的典型玩家是谁（各举 2 个）
  - 选这个边界的话，后面分析的重心会落在哪里

然后问我选哪一个。在我回答之前，不要开始分析。

注意：如果这几个候选之间的差别很小，说明你切分得不对，重切。真正有意义的切分，应该让不同候选的产业链结构明显不同。
```

### Step 1 · 结构层（`${choice}` 为用户选定的边界）

```
边界已确认：${choice}

现在做结构层。按下面的格式输出，不要写导语和总结。

【产业链分层】
从最上游到最终用户，分成若干层。每一层输出一行：

  层级 | 这一环干什么 | 代表公司(2-3家) | 它靠什么赚钱 | 收入确认时点 | evidence

其中：
- 「它靠什么赚钱」不许写"销售产品"。要写清楚收费方式：按件 / 按量 / 按项目 / 抽佣 / 订阅 / 赚差价 / 赚账期 / 赚返点，以及向谁收（很多行业里，付钱的人和用东西的人不是同一个）。
- 「收入确认时点」写：签约时 / 发货时 / 验收时 / 回款时 / 分期。这一栏后面会用到，不许留空，不知道就写「未获取」。

【这条链的特殊之处】
和一般制造业/服务业的标准链条比，这条链多了哪一环、少了哪一环、或者哪一环的位置不一样？只写差异，不写共性。

【我可能画错的地方】
你在分层时做了哪 3 个判断是可能错的？逐条列出。
```

### Step 2 · 三条流转链

```
基于上面的产业链，现在拆三条流转链。三条分开输出，不要合并。

【钱流】
从最终用户付出的 100 元开始，追踪它依次经过哪些环节。每一跳一行：

  从谁 → 到谁 | 这一跳留下多少(%) | 账期多长 | 谁先垫钱 | evidence

  账期和垫资这两栏是重点。如果不知道，写「未获取」并列入待验证清单，绝对不许用行业平均值或者常识填充。

【货流 / 服务交付流】
东西（或服务）实际怎么从生产端到达用户端。每一跳一行：

  从哪 → 到哪 | 谁掌握控制权 | 谁承担损耗和退货 | 时间多久 | evidence

  注意：货流和钱流的路径经常不一致（比如经销商不碰货只走账，或者平台碰货不碰账）。如果这条链和钱流路径不同，明确指出不同在哪。

【用户流】
用户是怎么被找到、被转化、被留住的。

  获客渠道 | 决策者是谁 | 付钱的是谁 | 使用的是谁 | 复购触发条件 | evidence

  「决策者 / 付钱的 / 使用的」三者是否分离，是这个行业的一个关键特征，必须明确回答是或否。

【三条链的错位】
三条链在哪一环出现了错位（控制权、资金、使用权不在同一方手里）？错位的地方通常就是这个行业的矛盾和机会所在，逐条指出。
```

### Step 3 · 议价能力

```
现在分析议价能力。

严格禁止使用以下表述：议价能力较强 / 较弱 / 相对集中 / 比较分散 / 处于优势地位 / 话语权较大。这些说法对任何行业都成立，等于没说。

对下面五个问题逐一作答，每个问题的答案必须是一个可以被验证的量。

1. 供应商议价
   问：如果现在要换掉这条链上最大的那个供应商，需要多久？成本上升多少？有几家可替代的？换供应商需要重新认证/备案吗？
   → 答案里必须出现具体的时间或金额。答不出来写「未获取」。

2. 客户议价
   问：客户从这一环切换到竞争对手，具体要付出什么代价？（合同违约金 / 数据迁移 / 重新培训 / 库存作废 / 关系重建 / 无代价）
   → 如果答案是"基本无代价"，那这一环的议价权就是零，直接这么写。

3. 替代品威胁
   问：这个行业的需求，上一次被另一种形态吃掉是什么时候？吃掉了多少？现在有没有正在发生的同类事情？
   → 要具体事件，不要"随着技术发展可能被替代"这种话。

4. 新进入者
   问：挡住新人的到底是什么？在下面几项里点名，不许全选：
   牌照/资质 | 起步资本量 | 渠道关系 | 手艺和经验 | 数据积累 | 规模成本 | 品牌信任 | 几乎没有壁垒
   → 点名之后回答：一个有钱有人的新玩家，从零到能接单要多久？

5. 同业竞争
   问：现有玩家是在同一个维度上打，还是分层错开？最近三年发生过几次价格战？由谁发起？结果如何？
   → 如果分层，说明分层的依据是什么（客户规模/地域/品类/服务深度）。

【综合结论】
在这条产业链上，利润最终沉淀在哪一环？为什么是这一环而不是别的？用上面五个答案来论证，不许引入新的理由。

【最脆弱的一环】
哪一环的议价权最容易在未来两年内被削弱？触发条件是什么？标注 evidence，如果是 C 档就明说这是推测。
```

### Step 4 · 约束/节奏/异常

```
最后一层。注意：这一层的规则和前面三层完全不同。

这三类信息在公开资料里极少存在，你大概率不知道。因此本步骤中：
- 你不许输出结论
- 你只许输出「我猜是这样」+「怎么验证」+「该问谁」
- 每一条都默认是 C 档，不需要标 evidence，但必须写清楚验证方法

【约束｜这个行业不能做的事】
分三类，每类给 3-5 条假设：
  法规约束：牌照、审批、准入、强制标准、监管红线
  物理约束：保质期、温控、运输半径、产能爬坡时间、设备切换成本
  习惯约束：账期惯例、返点潜规则、渠道保护、不能越过的关系

  格式：假设 | 如果这条成立会导致什么 | 怎么验证 | 该问谁

  重点：外行提出的蠢建议，几乎全部是撞在某条约束上，而且提出时完全不知道那条线存在。请你尽量列出那些"外行不会想到"的约束。

【节奏｜这个行业按什么周期运转】
  旺淡季在什么时候？| 客户的预算审批窗口？| 决策周期多长？| 库存周转天数？| 什么时候找客户是白费力气？

  格式同上：假设 | 影响 | 怎么验证 | 该问谁

  重点：外行最典型的失败不是做错事，是在错误的时间点做正确的事。

【异常｜看到什么意味着出事了】
  列出 8-10 个"数据组合"，每个说明它可能意味着什么。写成问句形式：「如果我看到 X 和 Y 同时发生，这在你们行业里通常意味着什么？」这些问句就是我要去问内行的清单。

【结尾必须输出：你还不知道的 10 件事】
这份报告读完之后，我最可能产生的错觉是什么？明确列出 10 件"这份报告没有回答、但对判断这个行业很关键"的事。

这一屏是整个任务里最重要的部分。不要客气，不要为了让报告显得完整而缩短这个清单。
```

### Step 5 · 从业者拷问（进入聊天模式时发送）

```
上面是我们一起做出的行业分析。现在切换角色。

你是一位在这个行业干了 12 年的从业者。我是一个刚接触这个行业、读了一些资料就来找你聊的人。

你的行为规则（优先级高于一切）：
1. 全程保持这个角色，不要跳出来解释你是 AI。
2. 禁止使用以下表达：很好的问题 / 你说得对 / 思路清晰 / 有道理 / 你已经抓住了重点。一次都不许出现。
3. 你的提问必须集中攻击上面报告里 evidence 为 B 和 C 的字段，以及「未获取」的那些栏。哪里心虚就问哪里。
4. 当我的回答里出现外行痕迹时，直接打断，指出是哪个词、哪个假设不对。不要等我说完，也不要先肯定再否定。
5. 一次只说 3 句话以内，像真人对话。

进行 15 轮之后，跳出角色，给我一份诊断：
  - 这份报告里哪几栏其实是空的，只是看起来被填满了
  - 我哪几处用词暴露了我是外行（原句 + 内行会怎么说）
  - 如果我只能再补 3 块知识，应该补哪 3 块

现在由你先开口，提出第一个问题。
```

## 三、API 对接（关键技术要求）

1. **端点与认证**：POST `{baseUrl}/v1/messages`，默认 `https://api.anthropic.com`。设置面板支持三方中转平台：可自定义 API 地址（自动清理尾部斜杠、自动拼 `/v1/messages`）、模型 ID 自由输入 + datalist 联想（三方常用 `anthropic/claude-opus-5` 这类前缀写法）、认证方式三选一——自动（官方用 `x-api-key`，三方用 `Authorization: Bearer`）/ 强制 x-api-key / 强制 Bearer。请求头始终带 `anthropic-version: 2023-06-01`；仅官方地址额外带 `anthropic-dangerous-direct-browser-access: true`（浏览器直连 CORS 必需）和 `anthropic-beta: server-side-fallback-2026-07-01`。
2. **请求体**：默认模型 `claude-opus-5`，`max_tokens: 64000`，`stream: true`，system 用上面的规则全文，`cache_control: {type:"ephemeral"}`（顶层自动缓存，多步共享前缀省钱）。仅官方地址加 `fallbacks: "default"`（安全分类器拦截时服务端自动换备用模型）。联网核实开关打开时加 `tools: [{type:"web_search_20260209", name:"web_search", max_uses:12}]`。
3. **SSE 流式解析**：按 `\n\n` 切事件、取 `data:` 行 JSON。要完整组装 content blocks：`content_block_start` 建块（深拷贝）；`text_delta` 追加正文并实时渲染；`input_json_delta` 累积 `partial_json` 字符串，`content_block_stop` 时 `JSON.parse` 回填 `input`；`thinking_delta`/`signature_delta` 也要累积保存（回传历史时 thinking 块必须原样带回）；`message_delta` 取 `stop_reason` 和输出 token 数；`message_start` 取输入 token 数（含 cache 读写）。遇到 `server_tool_use` 块时状态栏显示"检索中…（第 N 次）"。
4. **pause_turn 续传**：服务端检索多时会以 `stop_reason: "pause_turn"` 停下。此时把已组装的 assistant blocks 原样 push 进会话再发一次请求继续（API 允许连续同角色消息，自动合并），最多续 10 次。
5. **stop_reason 分支**：`refusal` → 显示"被安全分类器拒绝"；`max_tokens` → 显示"可能被截断"但保留内容；`end_turn` → 完成。

## 四、健壮性（三方中转平台实测必需）

1. **400 参数剥离重试**：三方网关可能不认识可选参数。收到 400 时看错误信息，命中 `fallback` → 删 `fallbacks` 重试；命中 `cache_control` → 删掉重试；命中 `tools/web_search` → 删掉重试并提示"该平台不支持检索工具，已降级（本步不再有 A 档）"。每个参数最多剥一次，其余 400 直接抛错。
2. **断流自动重试**：长流式请求常被中转网关中途掐断（fetch 抛 TypeError "network error"）。可重试条件：TypeError / network / 5xx / 529 / 429 / 流内 error 事件。重试前必须**把本节已流出的半截文本回滚到请求前的快照**（否则内容重复），429 等 10 秒，其余 2.5s/5s 退避，最多 2 次。用户主动 Abort 不重试。
3. **失败清理**：一轮彻底失败时，把这轮 push 进会话的 user 消息和半截 assistant 消息全部弹出（截断到发起前的长度），保证会话状态干净可重试。
4. **断点续跑**：流水线某步失败/被停止时不作废全局——在该步卡片下放「重试本步并继续」按钮，点击后清空该节正文、只重跑这一步，成功后自动继续后面的步骤（前面步骤的上下文都在会话里）。

## 五、界面与渲染

1. 手写一个轻量 Markdown 渲染器（先 HTML 转义再处理）：标题 #~####、**加粗**、\`行内代码\`、链接、围栏代码块、引用、分割线、无序列表、**有序列表必须保留原始序号**——模型输出的列表项之间常隔空行导致列表被切开，要把解析到的数字写进 `<li value="N">`，否则每项都显示成 "1."。表格是重点：`| a | b |` + 分隔行 → `<table>`，外面包 `overflow-x:auto` 容器。流式渲染用 requestAnimationFrame 节流（每帧整节重渲一次）。
2. 设置项（API 地址/Key/模型/认证方式/联网开关）全部存 localStorage，首次无 Key 自动展开设置面板。Key 只存本地，页面上写明。
3. 视觉：浅色纸感背景 + 深色模式（prefers-color-scheme），卡片式分节，绿色系强调色，状态栏跑动时加闪烁圆点。
4. 聊天模式：用户消息以带左边框的引用样式插入正文流（并追加进该节导出文本），输入框回车发送，placeholder 显示当前轮次。

## 六、验收清单

- [ ] 输入行业 → 出候选边界 → 点按钮或自定义输入 → 四步自动串行跑完 → 出现拷问模式入口
- [ ] 表格、空行分隔的有序列表（序号 1,2,…11,12 不重置）渲染正确
- [ ] 拔网线/断流后自动重试且无重复文本；重试耗尽后出现「重试本步并继续」按钮，点击后从断点续跑
- [ ] 换成三方平台地址 + Bearer + 前缀模型 ID 能正常跑；平台不支持 web_search 时自动降级并提示
- [ ] 停止按钮立即中断；导出 .md 内容完整；token 用量实时累计
